数据库原理期末作业

第一部分　PetStore数据库的设计

# 一、概念结构设计（ER图）

根据宠物商店电子商务系统的业务逻辑，对需求进行分析后，抽取出以下实体及其属性，并确定实体之间的联系，最终绘制 E-R 图。

## 1.1 实体分析

系统包含 6 个实体：用户、商品分类、商品、购物车、订单、订单明细。各实体属性及主键如下表所示。

|  |  |  |
| --- | --- | --- |
| **实体** | **包含属性** | **主键** |
| 用户 | 用户号、用户名、密码、性别、邮箱、电话 | 用户号 |
| 商品分类 | 分类编号、分类名称 | 分类编号 |
| 商品 | 商品编号、商品名、商品介绍、市场价格、当前价格、数量、分类编号 | 商品编号 |
| 购物车 | 购物车编号、用户号、商品编号、数量 | 购物车编号 |
| 订单 | 订单号、用户号、订单日期、订单总价、是否已处理 | 订单号 |
| 订单明细 | 明细编号、订单号、商品编号、商品名、单价、数量 | 明细编号 |

表1-1　实体及属性汇总表

## 1.2 实体属性详细说明

用户：用户号（唯一标识）、用户名、密码、性别、邮箱、电话——来源于用户注册功能。

商品分类：分类编号（唯一标识）、分类名称——管理员使用，用于组织商品层级结构。

商品：商品编号（唯一标识）、商品名、商品介绍、市场价格、当前价格、数量、分类编号（外键，关联商品分类）——管理员录入的商品信息。

购物车：购物车编号（唯一标识）、用户号（外键，关联用户）、商品编号（外键，关联商品）、数量——记录用户临时选购的宠物商品。

订单：订单号（唯一标识）、用户号（外键，关联用户）、订单日期、订单总价、是否已处理——用户完成选购后提交的预订记录。

订单明细：明细编号（唯一标识）、订单号（外键，关联订单）、商品编号（外键，关联商品）、商品名、单价、数量——每张订单中每个商品的购买详情。

## 1.3 联系分析

实体之间存在 6 个一对多（1 : N）联系，汇总如下表。

|  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| **序号** | **实体1** | **实体2** | **联系类型** | **联系名称** | **说明** |
| 1 | 商品分类 | 商品 | 1 : N | 包含 | 一个分类下可包含多个商品，一个商品只属于一个分类 |
| 2 | 用户 | 购物车 | 1 : N | 添加 | 一个用户可添加多条购物车记录，每条记录属于一个用户 |
| 3 | 商品 | 购物车 | 1 : N | 被加入 | 一个商品可被多个用户加入购物车，每条记录对应一个商品 |
| 4 | 用户 | 订单 | 1 : N | 下达 | 一个用户可下达多张订单，每张订单只属于一个用户 |
| 5 | 订单 | 订单明细 | 1 : N | 拥有 | 一张订单包含多条明细，每条明细只属于一张订单 |
| 6 | 商品 | 订单明细 | 1 : N | 对应 | 一个商品可出现在多条订单明细中，每条明细对应一个商品 |

表1-2　实体间联系汇总表

## 1.4 E-R 图

PetStore 数据库的整体 E-R 结构如下图所示：

![](data:image/png;base64...)

图1-1　PetStore 数据库 E-R 图

系统核心围绕用户展开：管理员维护商品分类，每个分类下包含多种商品。用户注册后可按分类浏览商品，将心仪商品加入购物车（一条购物车记录关联一个用户和一个商品）。用户确认选购后提交订单，每张订单包含多条订单明细，每条明细记录所购商品、单价和数量，最终汇总生成订单总价。

# 二、逻辑结构设计（关系模型）

将 E-R 图转换为关系模型：每个实体转换为一个关系（表），实体的属性即为表的字段，实体的主键即为表的主键。实体间的 1 : N 联系通过在 N 端表中添加外键实现。最终得到 6 张表，各表的逻辑结构如下。

**表2-1　用户表（user）**

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **约束** | **说明** |
| 用户号 | CHAR(10) | NOT NULL | PRIMARY KEY | 用户唯一标识 |
| 用户名 | VARCHAR(20) | NOT NULL | — | 用户昵称 |
| 密码 | VARCHAR(20) | NOT NULL | — | 登录密码 |
| 性别 | CHAR(2) | NULL | — | 男 / 女 |
| 邮箱 | VARCHAR(50) | NULL | — | 电子邮箱地址 |
| 电话 | CHAR(11) | NULL | — | 联系电话 |

**表2-2　商品分类表（category）**

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **约束** | **说明** |
| 分类编号 | CHAR(10) | NOT NULL | PRIMARY KEY | 分类唯一标识 |
| 分类名称 | VARCHAR(20) | NOT NULL | — | 如"狗""猫""鱼""鸟" |

**表2-3　商品表（product）**

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **约束** | **说明** |
| 商品编号 | CHAR(10) | NOT NULL | PRIMARY KEY | 商品唯一标识 |
| 商品名 | VARCHAR(50) | NOT NULL | — | 商品名称 |
| 商品介绍 | TEXT | NULL | — | 商品详细描述 |
| 市场价格 | DECIMAL(10,2) | NULL | — | 市场参考价 |
| 当前价格 | DECIMAL(10,2) | NOT NULL | — | 本站实际售价 |
| 数量 | INT | NOT NULL | — | 库存数量 |
| 分类编号 | CHAR(10) | NOT NULL | FOREIGN KEY | 外键，关联 category(分类编号) |

**表2-4　购物车表（cart）**

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **约束** | **说明** |
| 购物车编号 | INT | NOT NULL | PRIMARY KEY | 自增，购物车记录唯一标识 |
| 用户号 | CHAR(10) | NOT NULL | FOREIGN KEY | 外键，关联 user(用户号) |
| 商品编号 | CHAR(10) | NOT NULL | FOREIGN KEY | 外键，关联 product(商品编号) |
| 数量 | INT | NOT NULL | — | 选购数量，默认为 1 |

**表2-5　订单表（order）**

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **约束** | **说明** |
| 订单号 | CHAR(14) | NOT NULL | PRIMARY KEY | 如日期+序号，唯一标识 |
| 用户号 | CHAR(10) | NOT NULL | FOREIGN KEY | 外键，关联 user(用户号) |
| 订单日期 | DATETIME | NOT NULL | — | 下单时间 |
| 订单总价 | DECIMAL(10,2) | NOT NULL | — | 订单金额合计 |
| 是否已处理 | TINYINT(1) | NOT NULL | DEFAULT 0 | 0=未处理，1=已处理 |

**表2-6　订单明细表（order\_detail）**

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **约束** | **说明** |
| 明细编号 | INT | NOT NULL | PRIMARY KEY | 自增，明细唯一标识 |
| 订单号 | CHAR(14) | NOT NULL | FOREIGN KEY | 外键，关联 order(订单号) |
| 商品编号 | CHAR(10) | NOT NULL | FOREIGN KEY | 外键，关联 product(商品编号) |
| 商品名 | VARCHAR(50) | NOT NULL | — | 冗余存储，便于查询展示 |
| 单价 | DECIMAL(10,2) | NOT NULL | — | 购买时的实际单价 |
| 数量 | INT | NOT NULL | — | 购买数量 |

## 2.1 关系模式总结

上述 6 张表的关系模式可简洁表示为（带下划线者为主键）：

用户（用户号，用户名，密码，性别，邮箱，电话）

商品分类（分类编号，分类名称）

商品（商品编号，商品名，商品介绍，市场价格，当前价格，数量，分类编号）

购物车（购物车编号，用户号，商品编号，数量）

订单（订单号，用户号，订单日期，订单总价，是否已处理）

订单明细（明细编号，订单号，商品编号，商品名，单价，数量）

第二部分　数据库设计规范化

# 一、原始表分析

某建筑公司定期制定的工资报表如下：

**表1　某建筑公司原始工资报表**

|  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **工程号** | **工程名称** | **职工号** | **姓名** | **职务** | **小时工资率** | **工时** | **实发工资** |
| A1 | 花园大厦 | 1001 | 齐光明 | 工程师 | 65 | 13 | 845.00 |
| A1 | 花园大厦 | 1002 | 李思歧 | 技术员 | 60 | 16 | 960.00 |
| A1 | 花园大厦 | 1004 | 葛雨洪 | 技术员 | 60 | 19 | 1140.00 |
| \*小计\* | \*2945.00\* |  |  |  |  |  |  |
| A2 | 立交桥 | 1001 | 齐光明 | 工程师 | 65 | 15 | 975.00 |
| A2 | 立交桥 | 1003 | 鞠明亮 | 工人 | 55 | 17 | 935.00 |
| \*小计\* | \*1910.00\* |  |  |  |  |  |  |
| A3 | 临江饭店 | 1002 | 李思歧 | 技术员 | 60 | 18 | 1080.00 |
| A3 | 临江饭店 | 1004 | 葛雨洪 | 技术员 | 60 | 14 | 840.00 |
| \*小计\* | \*1920.00\* |  |  |  |  |  |  |

从表中可看出，公司目前有3个工程项目（A1花园大厦、A2立交桥、A3临江饭店）、4名员工（1001齐光明、1002李思歧、1003鞠明亮、1004葛雨洪）、3种职务（工程师、技术员、工人）。实发工资 = 小时工资率 × 工时。

此原始表将所有信息集中在一张表中，存在以下严重的数据库设计问题：

## （1）数据冗余

**工程信息重复**：工程号A1对应3名员工，"花园大厦"这个工程名称在3行中重复存储。同理，"立交桥"重复2次，"临江饭店"重复2次。

**员工信息重复**：齐光明（1001）参与A1和A2两个工程，其姓名"齐光明"、职务"工程师"在2行中重复；李思歧（1002）和葛雨洪（1004）同样各参与2个工程，其信息各重复2次。

**工资率信息重复**：小时工资率由职务决定（工程师=65，技术员=60，工人=55）。表中4名员工中2人为技术员，"60"这个小时工资率在技术员的4条记录中重复出现。

## （2）插入异常

**新工程无法插入**：当公司承接新工程但尚未分配施工人员时，由于表中主键为(工程号, 职工号)，职工号不能为空，导致无法插入仅包含工程信息的记录。

**新员工无法插入**：当公司招聘新员工（如一名新工程师）但尚未分配到具体工程时，由于工程号不能为空，无法插入该员工的个人信息和职务工资率。

## （3）删除异常

**删除工程导致员工信息丢失**：如果工程A2完工后删除相关记录，鞠明亮（1003）只参与了A2这一个工程，其个人信息将一并丢失。

**删除员工导致工程信息丢失**：如果从工程A1中移除全部3名施工人员，A1"花园大厦"的所有信息将丢失。

## （4）更新异常

**工资率更新复杂**：若工程师的小时工资率从65调整为70，需要更新所有工程师（此处为齐光明）的每条工时记录（齐光明有A1和A2两条记录），更新代价大且容易遗漏导致数据不一致。

**员工信息更新复杂**：若某员工职务变更（如李思歧从技术员升为工程师），需同步更新其参与的所有工程记录中的职务和小时工资率。

# 二、函数依赖分析

去除报表格式中的小计行后，原始数据表的属性集合为：

U = {工程号, 工程名称, 职工号, 姓名, 职务, 小时工资率, 工时, 实发工资}

原始表的主键为复合主键 **(工程号, 职工号)**，因为只有同时知道工程号和职工号，才能唯一确定一条工时记录。

## （1）完全函数依赖

完全函数依赖是指属性完全依赖于主键的全部，而非主键的一部分。

**(工程号, 职工号) → 工时**：工时由具体的工程和具体的职工共同决定，即某职工在某工程上工作了多少小时。

**(工程号, 职工号) → 实发工资**：实发工资 = 小时工资率 × 工时。虽然小时工资率由职务决定，但实发工资还依赖于工时（该职工在该工程的具体工时），因此实发工资完全函数依赖于(工程号, 职工号)。

## （2）部分函数依赖

部分函数依赖是指属性只依赖于复合主键的一部分，而非全部。

**工程号 → 工程名称**：工程名称只由工程号决定，与哪个职工无关。例如，工程号A1唯一确定工程名称"花园大厦"。

**职工号 → 姓名**：员工姓名只由职工号决定。例如，职工号1001唯一确定姓名"齐光明"。

-**职工号 → 职务**：员工职务只由职工号决定。例如，职工号1003唯一确定职务"工人"。

## （3）传递函数依赖

传递函数依赖是指非主属性通过另一个非主属性间接依赖于主键。

- **职工号 → 职务 → 小时工资率**：小时工资率由职务决定（工程师=65，技术员=60，工人=55），而职务依赖于职工号。因此，小时工资率通过职务传递依赖于职工号。即：职工号 → 职务，职务 → 小时工资率，故 **职工号 → 小时工资率**（传递依赖）。

函数依赖关系总结如下：

(工程号, 职工号) → 工时 （完全函数依赖）

(工程号, 职工号) → 实发工资 （完全函数依赖）

工程号 → 工程名称 （部分函数依赖）

职工号 → 姓名 （部分函数依赖）

职工号 → 职务 （部分函数依赖）

职务 → 小时工资率 （决定关系）

职工号 → 小时工资率 （传递函数依赖：职工号 → 职务 → 小时工资率）

# 三、规范化过程

## 第一步：确认原始表满足1NF（第一范式）

**1NF定义：** 关系中的每个分量必须是不可再分的数据项（原子性），且不存在重复组。

**原始表是否满足1NF？**

逐项检查原始报表（忽略小计汇总行）：

工程号：每个单元格只有一个值（A1/A2/A3）。

工程名称：每个单元格只有一个值。

职工号：每个单元格只有一个职工号。

姓名、职务、小时工资率、工时、实发工资：均为单一原子值。

**结论：原始表已满足1NF。** 每个字段都是原子数据，每行代表一个独立的"职工-工程"工时记录。不存在一个字段包含多个值的情况。

原始表在1NF下的关系模式为：

原始工资表(工程号, 工程名称, 职工号, 姓名, 职务, 小时工资率, 工时, 实发工资)

PRIMARY KEY (工程号, 职工号)

## 第二步：使表达到2NF（第二范式）

**2NF定义：** 在1NF基础上，消除非主属性对主键的**部分函数依赖**。

**当前1NF表中存在的部分函数依赖：**

|  |  |
| --- | --- |
| **部分函数依赖** | **实际问题示例** |
| 工程号 → 工程名称 | "花园大厦"在A1的3条记录中重复 |
| 职工号 → 姓名 | "齐光明"在其2条记录中重复 |
| 职工号 → 职务 | "工程师"在齐光明的2条记录中重复 |

**部分依赖导致的问题：**

数据冗余：如上表所示，工程名称、员工姓名和职务在每条工时记录中都重复存储。

更新异常：修改工程名称需更新多行；修改员工职务需更新多行。

插入/删除异常：如前述分析。

**分解原则：** 将部分函数依赖的属性与它们所依赖的主键部分分离，形成独立的关系表。

**分解方案（无损分解）：**

工程表(工程号 PK, 工程名称)

-- 工程号 → 工程名称（消除工程信息冗余）

员工表(职工号 PK, 姓名, 职务, 小时工资率)

-- 职工号 → 姓名, 职务, 小时工资率（消除员工信息冗余）

-- 注意：小时工资率仍有传递依赖问题，将在3NF阶段处理

工时记录表(工程号 PK, 职工号 PK, 工时, 实发工资)

-- (工程号, 职工号) → 工时, 实发工资（完全函数依赖，已满足2NF）

**验证：** 各表的每个非主属性都完全函数依赖于其主键，**达到2NF**。

工程表：工程名称完全依赖于工程号。✅

员工表：姓名、职务、小时工资率均完全依赖于职工号。✅ 工时记录表：工时、实发工资完全依赖于(工程号, 职工号)。✅

## 第三步：使表达到3NF（第三范式）

**3NF定义：** 在2NF基础上，消除非主属性对主键的**传递函数依赖**。

**当前2NF表中存在的传递函数依赖：**

在员工表中：

**职工号 → 职务**（直接依赖）

**职务 → 小时工资率**（职务决定工资率：工程师→65，技术员→60，工人→55）

因此：**职工号 → 小时工资率**（传递依赖：职工号 → 职务 → 小时工资率）

**传递依赖导致的问题：**

**更新异常**：若工程师小时工资率从65调整为70，需更新所有工程师（齐光明等）的记录；如果某员工职务变更，需同时修改职务和小时工资率两个字段，易致不一致。

**插入异常**：若公司新设"项目经理"职务并确定小时工资率为80，但尚未招聘该职务的员工，则80这个小时工资率无从存储。

**删除异常**：若某职务仅有一名员工且该员工离职被删除，则该职务的小时工资率信息一并丢失。

**分解原则：** 将传递依赖中的决定因素（职务 → 小时工资率）分离，形成独立的职务表。

**分解方案（无损分解）：**

工程表(工程号 PK, 工程名称)

职务表(职务编号 PK, 职务名称, 小时工资率)

-- 职务 → 小时工资率（消除传递依赖，职务信息独立管理）

员工表(职工号 PK, 姓名, 性别, 职务编号 FK REFERENCES 职务表(职务编号))

-- 职工号 → 姓名, 职务编号（员工基本信息）

-- 性别来自业务规则第②条要求，属于员工固有属性

工时记录表(工程号 PK, 职工号 PK, 工时, 实发工资)

-- (工程号, 职工号) → 工时, 实发工资

**验证：所有表达到3NF** ✅

- 工程表：工程号 → 工程名称，无传递依赖。✅

- 职务表：职务编号 → 职务名称, 小时工资率，无传递依赖。✅

- 员工表：职工号 → 姓名, 性别, 职务编号，职务编号是外键（非主属性之间无依赖关系）。✅

- 工时记录表：(工程号, 职工号) → 工时, 实发工资，实发工资 = 小时工资率 × 工时（计算字段，小时工资率通过职务表获取，不构成传递依赖）。✅

# 四、最终数据库设计

经过规范化（1NF → 2NF → 3NF），最终得到**四张表**，以下为完整设计：

## 工程表（xiangmu）

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **约束** | **说明** |
| 工程号 | CHAR(6) | PRIMARY KEY | 工程唯一标识，如A1、A2、A3 |
| 工程名称 | VARCHAR(50) | NOT NULL | 如"花园大厦""立交桥" |

**主键：** 工程号

**对应数据示例：**

|  |  |
| --- | --- |
| **工程号** | **工程名称** |
| A1 | 花园大厦 |
| A2 | 立交桥 |
| A3 | 临江饭店 |

## 职务表（zhiwu）

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **约束** | **说明** |
| 职务编号 | CHAR(4) | PRIMARY KEY | 职务唯一标识 |
| 职务名称 | VARCHAR(20) | NOT NULL | 如"工程师""技术员""工人" |
| 小时工资率 | DECIMAL(8,2) | NOT NULL | 该职务对应的每小时工资 |

**主键：** 职务编号

**对应数据示例：**

|  |  |  |
| --- | --- | --- |
| **职务编号** | **职务名称** | **小时工资率** |
| Z01 | 工程师 | 65.00 |
| Z02 | 技术员 | 60.00 |
| Z03 | 工人 | 55.00 |

## 员工表（yuangong）

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **约束** | **说明** |
| 职工号 | CHAR(6) | PRIMARY KEY | 员工唯一标识 |
| 姓名 | VARCHAR(20) | NOT NULL | 员工姓名 |
| 性别 | CHAR(2) | NOT NULL | 男/女（来自业务规则第②条） |
| 职务编号 | CHAR(4) | FOREIGN KEY | 外键，关联职务表(职务编号) |

**主键：** 职工号

**外键：** 职务编号 REFERENCES 职务表(职务编号)

**对应数据示例：**

|  |  |  |  |
| --- | --- | --- | --- |
| **职工号** | **姓名** | **性别** | **职务编号** |
| 1001 | 齐光明 | 男 | Z01 |
| 1002 | 李思歧 | 男 | Z02 |
| 1003 | 鞠明亮 | 男 | Z03 |
| 1004 | 葛雨洪 | 男 | Z02 |

## 工时记录表（gongshi）

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **约束** | **说明** |
| 工程号 | CHAR(6) | PRIMARY KEY (复合) | 外键，关联工程表(工程号) |
| 职工号 | CHAR(6) | PRIMARY KEY (复合) | 外键，关联员工表(职工号) |
| 工时 | DECIMAL(5,1) | NOT NULL | 该职工在该工程的工作小时数 |

**主键：** (工程号, 职工号) —— 复合主键

**外键：** 工程号 REFERENCES 工程表(工程号)；职工号 REFERENCES 员工表(职工号)

**规范化总结：** 经过 1NF → 2NF → 3NF 的逐步分解，原始的一张工资报表被规范化为四张表（工程表、职务表、员工表、工时记录表），彻底消除了数据冗余和插入/删除/更新异常，满足第三范式要求。

第三部分　SQL实验

以下所有SQL语句均采用MySQL语法，基于EM数据库。

# 一、创建数据库

-- 创建EM数据库

CREATE DATABASE EM

DEFAULT CHARACTER SET utf8mb4

DEFAULT COLLATE utf8mb4\_general\_ci;

-- 使用EM数据库

USE EM;

# 二、创建数据表

## （1）departments 表（部门信息表）

CREATE TABLE departments (

部门编号 CHAR(3) NOT NULL,

部门名称 CHAR(20) NOT NULL,

备注 TEXT NULL,

PRIMARY KEY (部门编号)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **说明** |
| 部门编号 | CHAR(3) | NOT NULL | 主键 |
| 部门名称 | CHAR(20) | NOT NULL |  |
| 备注 | TEXT | NULL |  |

## （2）employees 表（员工信息表）

CREATE TABLE employees (

员工编号 CHAR(6) NOT NULL,

姓名 CHAR(10) NOT NULL,

学历 CHAR(4) NOT NULL,

出生日期 DATE NOT NULL,

性别 CHAR(2) NOT NULL,

工作年限 INT NULL,

地址 VARCHAR(20) NULL,

电话号码 CHAR(12) NULL,

员工部门号 CHAR(3) NULL,

PRIMARY KEY (员工编号),

FOREIGN KEY (员工部门号) REFERENCES departments(部门编号)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **说明** |
| 员工编号 | CHAR(6) | NOT NULL | 主键 |
| 姓名 | CHAR(10) | NOT NULL |  |
| 学历 | CHAR(4) | NOT NULL |  |
| 出生日期 | DATE | NOT NULL |  |
| 性别 | CHAR(2) | NOT NULL | 1=男，0=女 |
| 工作年限 | INT | NULL |  |
| 地址 | VARCHAR(20) | NULL |  |
| 电话号码 | CHAR(12) | NULL |  |
| 员工部门号 | CHAR(3) | NULL | 外键，关联departments |

## （3）salary 表（员工薪水情况表）

CREATE TABLE salary (

员工编号 CHAR(6) NOT NULL,

收入 FLOAT(8,2) NOT NULL,

支出 FLOAT(8,2) NOT NULL,

PRIMARY KEY (员工编号),

FOREIGN KEY (员工编号) REFERENCES employees(员工编号)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

|  |  |  |  |
| --- | --- | --- | --- |
| **字段名** | **数据类型** | **允许为空** | **说明** |
| 员工编号 | CHAR(6) | NOT NULL | 主键，外键关联employees |
| 收入 | FLOAT(8,2) | NOT NULL |  |
| 支出 | FLOAT(8,2) | NOT NULL |  |

# 三、插入数据

## （1）departments 表数据插入

INSERT INTO departments (部门编号, 部门名称, 备注) VALUES

('1', '财务部', NULL),

('2', '人力资源部', NULL),

('3', '经理办公室', NULL),

('4', '研发部', NULL),

('5', '市场部', NULL);

## （2）employees 表数据插入

INSERT INTO employees (员工编号, 姓名, 学历, 出生日期, 性别, 工作年限, 地址, 电话号码, 员工部门号) VALUES

('000001', '王林', '大专', '1966-01-23', '1', 8, '中山路32-1-508', '83355668', '2'),

('010008', '伍容华', '本科', '1976-03-28', '1', 3, '北京东路100-2', '83321321', '1'),

('020010', '王向容', '硕士', '1982-12-09', '1', 2, '四牌楼10-0-108', '83792361', '1'),

('020018', '李丽', '大专', '1960-07-30', '0', 6, '中山东路102-2', '83413301', '1'),

('102201', '刘明', '本科', '1972-10-18', '1', 3, '虎踞路100-2', '83606608', '5'),

('102208', '朱俊', '硕士', '1965-09-28', '1', 2, '牌楼巷5-3-106', '84708817', '5'),

('108991', '钟敏', '硕士', '1979-08-10', '0', 4, '中山路10-3-105', '83346722', '3'),

('111006', '张石兵', '本科', '1974-10-01', '1', 1, '解放路34-1-203', '84563418', '5'),

('210678', '林涛', '大专', '1977-04-02', '1', 2, '中山北路24-35', '83467336', '3'),

('302566', '李玉珉', '本科', '1968-09-20', '1', 3, '热和路209-3', '58765991', '4'),

('308759', '叶凡', '本科', '1978-11-18', '1', 2, '北京西路3-7-52', '83308901', '4'),

('504209', '陈林琳', '大专', '1969-09-03', '0', 5, '汉中路120-4-12', '84468158', '4');

## （3）salary 表数据插入

INSERT INTO salary (员工编号, 收入, 支出) VALUES

('000001', 2100.80, 123.09),

('010008', 1582.62, 88.03),

('020010', 2860.00, 198.00),

('020018', 2347.68, 180.00),

('102201', 2569.88, 185.65),

('102208', 1980.00, 100.00),

('108991', 3259.98, 281.52),

('111006', 1987.01, 79.58),

('210678', 2240.00, 121.00),

('302566', 2980.70, 210.20),

('308759', 2531.98, 199.08),

('504209', 2066.15, 108.00);

# 四、单表查询

## ① 查询employees表的学历，要求消除重复行

**题目：** 查询employees表中都有哪些学历（消除重复行）。

SELECT DISTINCT 学历

FROM employees;

## ② 计算每个雇员的年龄

**题目：** 计算每个雇员的年龄（年龄 = 当前年份 - 出生年份）。

SELECT

员工编号,

姓名,

出生日期,

YEAR(CURDATE()) - YEAR(出生日期) AS 年龄

FROM employees;

## ③ 查询员工姓名和性别（性别值转换显示）

**题目：** 查询employees表中员工的姓名和性别，要求性别值为1时显示为"男"，为0时显示为"女"。

SELECT

姓名,

CASE WHEN 性别 = '1' THEN '男'

WHEN 性别 = '0' THEN '女'

ELSE '未知'

END AS 性别

FROM employees;

## ④ 查询员工编号和电话号码（自定义列标题）

**题目：** 查询每个雇员的员工编号和电话号码，显示的列标题为id和telephone。

SELECT

员工编号 AS id,

电话号码 AS telephone

FROM employees;

## ⑦ 计算部门总数

**题目：** 计算部门总数。

SELECT COUNT(\*) AS 部门总数

FROM departments;

## ⑧ 显示员工的最高收入和最低收入

**题目：** 显示员工的最高收入和最低收入。

SELECT

MAX(收入) AS 最高收入,

MIN(收入) AS 最低收入

FROM salary;

## ⑨ 查询姓"王"且名字为3个字的员工所有信息

**题目：** 查询姓"王"且名字为3个字的员工所有信息。

SELECT \*

FROM employees

WHERE 姓名 LIKE '王\_\_';

## ⑩ 显示学历在本科以上（含本科）的女性员工信息

**题目：** 显示学历在本科以上（含本科）的女性员工的信息。

SELECT \*

FROM employees

WHERE 学历 IN ('本科', '硕士', '博士')

AND 性别 = '0';

## ⑪ 查找所有员工信息，只显示前三条

**题目：** 查找所有员工信息，只显示前3条。

SELECT \*

FROM employees

LIMIT 3;

# 五、多表查询

## ① 查询"李丽"和"朱俊"的基本情况和所工作的部门名称

**题目：** 查询"李丽"和"朱俊"的基本情况和所工作的部门名称。

SELECT

e.\*,

d.部门名称

FROM employees e

INNER JOIN departments d ON e.员工部门号 = d.部门编号

WHERE e.姓名 IN ('李丽', '朱俊');

## ② 左连接方式查询employees和departments表中的所有信息

**题目：** 左连接方式查询employees和departments表中的所有信息。

SELECT \*

FROM employees e

LEFT JOIN departments d ON e.员工部门号 = d.部门编号;

## ③ 查询研发部在1970年以前出生的员工姓名和薪水情况

**题目：** 查询研发部在1970年以前出生的员工姓名和薪水情况。

SELECT

e.姓名,

e.出生日期,

s.收入,

s.支出

FROM employees e

INNER JOIN departments d ON e.员工部门号 = d.部门编号

INNER JOIN salary s ON e.员工编号 = s.员工编号

WHERE d.部门名称 = '研发部'

AND e.出生日期 < '1970-01-01';

## ④ 查询员工的姓名、住址和收入水平

**题目：** 查询员工的姓名、住址和收入水平，要求2000元以下显示为"低收入"，2000~3000元显示为"中等收入"，3000元以上时显示为"高收入"。

SELECT

e.姓名,

e.地址,

s.收入,

CASE

WHEN s.收入 < 2000 THEN '低收入'

WHEN s.收入 BETWEEN 2000 AND 3000 THEN '中等收入'

WHEN s.收入 > 3000 THEN '高收入'

ELSE '未知'

END AS 收入水平

FROM employees e

INNER JOIN salary s ON e.员工编号 = s.员工编号;

# 六、分类汇总与排序

## ① 分别统计男性员工和女性员工人数

**题目：** 分别统计男性员工和女性员工人数。

SELECT

CASE WHEN 性别 = '1' THEN '男'

WHEN 性别 = '0' THEN '女'

ELSE '未知'

END AS 性别,

COUNT(\*) AS 人数

FROM employees

GROUP BY 性别;

## ② 将员工信息按照出生日期从大到小排列

**题目：** 将员工信息按照出生日期从大到小排列。

SELECT \*

FROM employees

ORDER BY 出生日期 DESC;

## ③ 按员工学历分组，查询每组有哪些员工

**题目：** 查询按员工学历分组，每组有哪些员工。

SELECT

学历,

GROUP\_CONCAT(姓名 ORDER BY 姓名 SEPARATOR '、') AS 员工名单,

COUNT(\*) AS 人数

FROM employees

GROUP BY 学历

ORDER BY 人数 DESC;

## ④ 按员工工作年限分组，统计各年限人数并按人数升序排列

**题目：** 按员工工作年限分组，统计各个工作年限的人数，并按人数从小到大排序。

SELECT

工作年限,

COUNT(\*) AS 人数

FROM employees

GROUP BY 工作年限

ORDER BY 人数 ASC;

# 七、增删改操作

## ① 插入：公司新成立一个生产部

**题目：** 公司新成立一个生产部，部门代码为6，备注"代工"。

INSERT INTO departments (部门编号, 部门名称, 备注)

VALUES ('6', '生产部', '代工');

## ② 插入：销售部新进1名成员

**题目：** 销售部新进1名成员常皓，信息如下：常皓，硕士，1988-01-19，男，7年，七贤路35号，83728734。

由于当前departments表中不存在"销售部"（现有部门为1-5，且题目①已新增部门编号6为生产部），需先将销售部添加到部门表中，再插入员工信息：

-- 第一步：新增销售部

INSERT INTO departments (部门编号, 部门名称, 备注)

VALUES ('7', '销售部', '负责销售业务');

-- 第二步：插入新员工常皓，归属销售部（部门编号=7）

INSERT INTO employees (员工编号, 姓名, 学历, 出生日期, 性别, 工作年限, 地址, 电话号码, 员工部门号)

VALUES ('100010', '常皓', '硕士', '1988-01-19', '1', 7, '七贤路35号', '83728734', '7');

## ③ 更新：林涛工资增加500元

**题目：** 因工作能力出色，林涛工资增加500元。

UPDATE salary

SET 收入 = 收入 + 500

WHERE 员工编号 = (

SELECT 员工编号 FROM employees WHERE 姓名 = '林涛'

);

或者使用多表更新语法：

UPDATE salary s

INNER JOIN employees e ON s.员工编号 = e.员工编号

SET s.收入 = s.收入 + 500

WHERE e.姓名 = '林涛';

## ④ 删除：张石兵辞职，删除所有相关信息

**题目：** 张石兵辞职，请将跟他有关的所有信息删除。

-- 注意：由于salary表通过外键关联employees表，需先删除子表记录

-- 首先删除salary表中的记录

DELETE FROM salary

WHERE 员工编号 = (

SELECT 员工编号 FROM employees WHERE 姓名 = '张石兵'

);

-- 然后删除employees表中的记录

DELETE FROM employees

WHERE 姓名 = '张石兵';

# 附录：完整SQL脚本

将以上所有SQL语句整合为可一次性执行的脚本：

-- ============================================

-- EM数据库：员工工资管理系统 完整SQL脚本

-- MySQL版本

-- ============================================

-- 1. 创建数据库

CREATE DATABASE IF NOT EXISTS EM

DEFAULT CHARACTER SET utf8mb4

DEFAULT COLLATE utf8mb4\_general\_ci;

USE EM;

-- 2. 创建数据表

CREATE TABLE departments (

部门编号 CHAR(3) NOT NULL,

部门名称 CHAR(20) NOT NULL,

备注 TEXT NULL,

PRIMARY KEY (部门编号)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE employees (

员工编号 CHAR(6) NOT NULL,

姓名 CHAR(10) NOT NULL,

学历 CHAR(4) NOT NULL,

出生日期 DATE NOT NULL,

性别 CHAR(2) NOT NULL,

工作年限 INT NULL,

地址 VARCHAR(20) NULL,

电话号码 CHAR(12) NULL,

员工部门号 CHAR(3) NULL,

PRIMARY KEY (员工编号),

FOREIGN KEY (员工部门号) REFERENCES departments(部门编号)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE salary (

员工编号 CHAR(6) NOT NULL,

收入 FLOAT(8,2) NOT NULL,

支出 FLOAT(8,2) NOT NULL,

PRIMARY KEY (员工编号),

FOREIGN KEY (员工编号) REFERENCES employees(员工编号)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. 插入部门数据

INSERT INTO departments (部门编号, 部门名称, 备注) VALUES

('1', '财务部', NULL),

('2', '人力资源部', NULL),

('3', '经理办公室', NULL),

('4', '研发部', NULL),

('5', '市场部', NULL);

-- 4. 插入员工数据

INSERT INTO employees (员工编号, 姓名, 学历, 出生日期, 性别, 工作年限, 地址, 电话号码, 员工部门号) VALUES

('000001', '王林', '大专', '1966-01-23', '1', 8, '中山路32-1-508', '83355668', '2'),

('010008', '伍容华', '本科', '1976-03-28', '1', 3, '北京东路100-2', '83321321', '1'),

('020010', '王向容', '硕士', '1982-12-09', '1', 2, '四牌楼10-0-108', '83792361', '1'),

('020018', '李丽', '大专', '1960-07-30', '0', 6, '中山东路102-2', '83413301', '1'),

('102201', '刘明', '本科', '1972-10-18', '1', 3, '虎踞路100-2', '83606608', '5'),

('102208', '朱俊', '硕士', '1965-09-28', '1', 2, '牌楼巷5-3-106', '84708817', '5'),

('108991', '钟敏', '硕士', '1979-08-10', '0', 4, '中山路10-3-105', '83346722', '3'),

('111006', '张石兵', '本科', '1974-10-01', '1', 1, '解放路34-1-203', '84563418', '5'),

('210678', '林涛', '大专', '1977-04-02', '1', 2, '中山北路24-35', '83467336', '3'),

('302566', '李玉珉', '本科', '1968-09-20', '1', 3, '热和路209-3', '58765991', '4'),

('308759', '叶凡', '本科', '1978-11-18', '1', 2, '北京西路3-7-52', '83308901', '4'),

('504209', '陈林琳', '大专', '1969-09-03', '0', 5, '汉中路120-4-12', '84468158', '4');

-- 5. 插入薪水数据

INSERT INTO salary (员工编号, 收入, 支出) VALUES

('000001', 2100.80, 123.09),

('010008', 1582.62, 88.03),

('020010', 2860.00, 198.00),

('020018', 2347.68, 180.00),

('102201', 2569.88, 185.65),

('102208', 1980.00, 100.00),

('108991', 3259.98, 281.52),

('111006', 1987.01, 79.58),

('210678', 2240.00, 121.00),

('302566', 2980.70, 210.20),

('308759', 2531.98, 199.08),

('504209', 2066.15, 108.00);